ANTHROPIC_API_KEY=sk-ant-...

# SMS / Voice via Twilio (optional). Get these at console.twilio.com.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# Email via SMTP (optional). For Gmail, use an App Password
# (https://myaccount.google.com/apppasswords), NOT your regular password.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
